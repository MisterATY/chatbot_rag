# API server
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6

# Vector DB
qdrant-client>=1.7.0

# Embeddings & reranker
sentence-transformers>=2.2.0
torch>=2.0.0
transformers>=4.57.0

# Qwen3-VL embedding (optional; used when EMBEDDING_BACKEND=qwen3-vl)
qwen-vl-utils>=0.0.14

# Qwen3-Embedding GGUF (optional; used when QWEN_EMBEDDING_GGUF_PATH is set)
llama-cpp-python>=0.2.0

# Utilities
numpy>=1.24.0
requests>=2.28.0

# DOCX / TXT ingestion
python-docx>=1.0.0
